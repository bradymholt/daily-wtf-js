var a = [,];
a.length; //1, some element exists
a.indexOf(a[0]); //-1
// We have the array with non-zero length, which doesn't contain its first element. WTF?

// Source: frontenddeveloping (https://github.com/frontenddeveloping)